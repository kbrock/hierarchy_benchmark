source 'https://rubygems.org'

gem 'activerecord', require: 'active_record' #, '~> 5.1.5'
# Use postgresql as the database for Active Record
gem 'pg', '>= 0.18', '< 2.0'
gem 'ancestry', path: "#{ENV["HOME"]}/src/gems/ancestry"

gem "benchmark-ips", "~> 2.7.2" # may be different version than rails benchmark
# temporary:
gem "byebug"
